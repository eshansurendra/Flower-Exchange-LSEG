#ifndef VALIDATE_ORDER_H
#define VALIDATE_ORDER_H

#include <iostream>
#include <set>
#include <string>
#include "handlecsv.h" // Include the CSVRow struct and trim function

// Function to validate an order based on given criteria
bool validateOrder(CSVRow& row);

#endif // VALIDATE_ORDER_H
